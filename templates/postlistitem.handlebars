<a href="#" onclick="$('.list-group-item').removeClass('active'); $(this).addClass('active'); loadPost({{ ID }});"
   class="list-group-item">
<h4 class="list-group-item-heading">{{ title }}</h4>

    <p class="list-group-item-text">created on {{ formatDate date }}</p>
</a>